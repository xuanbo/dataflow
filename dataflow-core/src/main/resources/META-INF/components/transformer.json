{
  "group": "BASIC",
  "name": "TRANSFORMER_SQL",
  "text": "转换",
  "argument": {
    "input": [
      {
        "name": "table",
        "text": "物化视图",
        "type": "string",
        "component": "input",
        "required": true,
        "remark": "读取物化视图"
      },
      {
        "name": "select",
        "text": "查询列",
        "type": "string",
        "component": "textarea",
        "required": true,
        "remark": "SQL 查询列（支持别名、函数），无需写 SELECT 关键字"
      },
      {
        "name": "groupBy",
        "text": "分组",
        "type": "string",
        "component": "textarea",
        "required": false,
        "remark": "SQL GROUP BY 分组语句，无需写 GROUP BY 关键字"
      }
    ],
    "output": [
      {
        "name": "table",
        "text": "物化视图",
        "type": "string",
        "component": "input",
        "required": true,
        "remark": "结果物化视图"
      }
    ]
  }
}