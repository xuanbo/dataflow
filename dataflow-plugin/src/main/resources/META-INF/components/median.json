{
  "group": "ALGORITHM",
  "name": "ALGORITHM_MEDIAN",
  "text": "中位数",
  "argument": {
    "input": [
      {
        "name": "table",
        "text": "物化视图",
        "type": "string",
        "component": "input",
        "required": true,
        "remark": "读取物化视图"
      },
      {
        "name": "col",
        "text": "字段列",
        "type": "string",
        "component": "input",
        "required": true,
        "remark": "求中位数的字段列，必须是数值"
      },
      {
        "name": "partition",
        "text": "分区",
        "type": "number",
        "component": "input",
        "required": false,
        "remark": "分区数量，默认4，该数值会影响计算性能"
      }
    ],
    "output": [
      {
        "name": "table",
        "text": "物化视图",
        "type": "string",
        "component": "input",
        "required": true,
        "remark": "结果物化视图"
      }
    ]
  }
}